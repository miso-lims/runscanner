title: Run Scanner Documentation
email: alexis.varsava@oicr.on.ca
description: >- # this means to ignore newlines until "baseurl:"
        Run Scanner is a tool for monitoring output data from sequencing equipment.
baseurl: "/" # the subpath of your site, e.g. /blog

category-list: ['User Manual']

markdown: kramdown
theme: minima
plugins:
  - jekyll-feed

# Exclude from processing.
# The following items will not be processed, by default. Create a custom list
# to override the default setting.
# exclude:
#   - Gemfile
#   - Gemfile.lock
#   - node_modules
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/
